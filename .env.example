# Origin only (no /quran suffix). Must be your API host (Nest/serverless), not the extension app or a *.vercel.app preview URL.
# Example API: https://quran-chrome-extension-api.vercel.app
VITE_API_URL=http://localhost:3000
VITE_EXTENSION_SECRET=